<Page x:Class="himchistka.practic.Views.CatalogPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:views="clr-namespace:himchistka.practic.Views"
      Title="CatalogPage">
    <Grid Background="{StaticResource BrandPageBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="78"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource BrandHeroBrush}" Padding="28,20" CornerRadius="0,0,28,28">
            <Border.Effect>
                <DropShadowEffect BlurRadius="24" ShadowDepth="6" Opacity="0.18" Color="#102A43"/>
            </Border.Effect>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock Text="Crystal Clean" Foreground="White" FontWeight="Bold" FontSize="30"/>
                    <TextBlock Text="Профессиональная химчистка с бережным уходом и доставкой" Foreground="#DFFCF4" FontSize="14" Margin="0,4,0,0"/>
                </StackPanel>

                <Button Grid.Column="1" Content="Выйти" Command="{Binding LogoutCommand}" Background="White" Foreground="{StaticResource BrandNavyBrush}" BorderBrush="White"/>
            </Grid>
        </Border>

        <Border Grid.Row="1" Margin="28,18,28,10" Padding="18" Background="White" CornerRadius="18" BorderBrush="{StaticResource BrandLineBrush}" BorderThickness="1">
            <Border.Effect>
                <DropShadowEffect BlurRadius="18" ShadowDepth="4" Opacity="0.08" Color="#102A43"/>
            </Border.Effect>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="14"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0"
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         ToolTip="Введите название услуги или описание"/>

                <ComboBox Grid.Column="2"
                          ItemsSource="{Binding CategoryOptions}"
                          SelectedItem="{Binding SelectedCategory}"/>
            </Grid>
        </Border>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Padding="28,8,28,14">
            <ItemsControl ItemsSource="{Binding FilteredProducts}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <views:ProductCardView AddToCartCommand="{Binding DataContext.AddToCartCommand, RelativeSource={RelativeSource AncestorType=Page}}"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <Border Grid.Row="3" Background="White" Padding="18,12" BorderBrush="{StaticResource BrandLineBrush}" BorderThickness="1,1,1,0">
            <Border.Effect>
                <DropShadowEffect BlurRadius="18" ShadowDepth="-4" Opacity="0.06" Color="#102A43"/>
            </Border.Effect>
            <UniformGrid Columns="5" Rows="1">
                <Button Margin="6,0" Content="Заказы" Command="{Binding OpenOrderCommand}" Background="#EAF3FF" Foreground="{StaticResource BrandNavyBrush}" BorderBrush="#EAF3FF"/>
                <Button Margin="6,0" Content="Пользователь" Command="{Binding OpenProfileCommand}" Background="#EAF3FF" Foreground="{StaticResource BrandNavyBrush}" BorderBrush="#EAF3FF"/>
                <Button Margin="6,0" Content="Админ панель" Command="{Binding OpenUsersCommand}" Background="#EAF3FF" Foreground="{StaticResource BrandNavyBrush}" BorderBrush="#EAF3FF" Visibility="{Binding AdminButtonVisibility}"/>
                <Button Margin="6,0" Content="Услуги" Command="{Binding OpenProductsCommand}" Background="#EAF3FF" Foreground="{StaticResource BrandNavyBrush}" BorderBrush="#EAF3FF"/>
                <Button Margin="6,0" Content="Корзина" Command="{Binding OpenCartCommand}" Background="{StaticResource BrandHeroBrush}" Foreground="White" BorderBrush="{StaticResource BrandBlueBrush}"/>
            </UniformGrid>
        </Border>
    </Grid>
</Page>
