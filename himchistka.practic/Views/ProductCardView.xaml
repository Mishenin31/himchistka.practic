<UserControl x:Class="himchistka.practic.Views.ProductCardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:models="clr-namespace:himchistka.practic.Models"
             x:Name="Root">
    <Border Margin="0,0,0,10" Padding="12" Background="White" BorderBrush="#D5DBDB" BorderThickness="1" CornerRadius="6">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="220"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="140"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Width="100" Height="72" Background="#EBEDEF" CornerRadius="4" HorizontalAlignment="Left" VerticalAlignment="Center">
                <TextBlock Text="IMG" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#7B7D7D" FontWeight="Bold"/>
            </Border>

            <TextBlock Grid.Column="1" Text="{Binding Name}" VerticalAlignment="Center" FontWeight="SemiBold" FontSize="16" Margin="8,0"/>
            <TextBlock Grid.Column="2" Text="{Binding Description}" VerticalAlignment="Center" Foreground="#566573" Margin="8,0"/>
            <Button Grid.Column="3"
                    Content="В корзину"
                    Command="{Binding ElementName=Root, Path=AddToCartCommand}"
                    CommandParameter="{Binding}"
                    Padding="8,6"
                    VerticalAlignment="Center"/>
        </Grid>
    </Border>
</UserControl>
