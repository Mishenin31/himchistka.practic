<Window x:Class="himchistka.practic.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Order System"
        Height="720"
        Width="1200"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BrandPageBrush}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource BrandHeroBrush}" Padding="24,14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Width="38" Height="38" CornerRadius="12" Background="#33FFFFFF" Margin="0,0,12,0">
                        <TextBlock Text="✦" Foreground="White" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel>
                        <TextBlock Text="Crystal Clean" Foreground="White" FontWeight="Bold" FontSize="18" />
                        <TextBlock Text="{Binding HeaderTitle}" Foreground="#DFFCF4" FontSize="12" />
                    </StackPanel>
                </StackPanel>

                <Border Grid.Column="1" Margin="20,0,12,0" Padding="14,8" Background="#22FFFFFF" CornerRadius="12" VerticalAlignment="Center">
                    <TextBlock Text="{Binding CurrentUserName}" Foreground="White" FontWeight="SemiBold"/>
                </Border>
                <Button Grid.Column="2" Content="Выход" Command="{Binding LogoutCommand}" Background="White" Foreground="{StaticResource BrandNavyBrush}" BorderBrush="White" />
            </Grid>
        </Border>

        <Frame x:Name="MainFrame" Grid.Row="1" NavigationUIVisibility="Hidden" />
    </Grid>
</Window>
